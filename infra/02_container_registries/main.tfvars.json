{
  "environment_name": "${AZURE_ENV_NAME}",
  "location": "${AZURE_LOCATION}",
  "subscription_id": "${AZURE_SUBSCRIPTION_ID}",
  "principal_id": "${AZURE_PRINCIPAL_ID}",
  "github_actions_principal_id": "${GITHUB_ACTIONS_PRINCIPAL_ID}"
}
